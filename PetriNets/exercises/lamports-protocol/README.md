Structure:
    - lamport_complete.lola: lola file of the protocol after adding the A process
    - lamports1bit.xml: lamports protocol that can be opened with PIPE
    - lamports1bit.png: diagram of the net exported from PIPE
    - lola_sols.sh: commands to run to check the asked questions:
      1. deadlock-freedom
      2. process can be at multiple places at the same time
      3. both processes can reach their critical sections simultaneously